<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/context 
       http://www.springframework.org/schema/context/spring-context-3.0.xsd
       http://www.springframework.org/schema/tx 
       http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop-3.0.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd"
       default-lazy-init="true">
<!--
    <import resource="applicationContext-mq.xml"/>
-->

    <!--todo: 激活JSR-250 Common Java注解，为使用PostConstruct/PreDestroy -->
    <context:annotation-config/>
    <mvc:annotation-driven/>

    <!-- 扫描类包，将标注Spring注解的类自动转化Bean，同时完成Bean的注入 -->
    <context:component-scan base-package="com.future.dao4ora"/>

    <!--todo:配置属性变量文件的存放位置，用于@Value("${xxx}")注解中的占位符-->
    <context:property-placeholder
            location="WEB-INF/classes/jdbc/jdbc4oracle.properties"/>
    <!-- 配置数据源 -->
	<bean id="dataSource4ora" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close" 
		p:driverClassName="${ora.driver}"
		p:url="${ora.url}"
		p:username="${ora.username}"
		p:password="${ora.password}"
        p:initialSize="${ora.initialSize}"
        p:maxActive="${ora.maxActive}"
          p:maxIdle="${ora.maxIdle}"
          p:minIdle="${ora.minIdle}"
          p:maxWait="${ora.maxWait}"
    />

    <!-- spring和MyBatis完美整合，不需要mybatis的配置映射文件 -->
    <bean id="sqlSessionFactory4ora" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource4ora" />
        <!-- 自动扫描mapping.xml文件 -->
        <property name="mapperLocations" value="classpath:com/future/dao4ora/sqlmap/*.xml"></property>
        <!--page helper设置-->
        <property name="plugins">
            <array>
                <bean class="com.github.pagehelper.PageHelper">
                    <property name="properties">
                        <value>
                            dialect=oracle
                        </value>
                    </property>
                </bean>
            </array>
        </property>
    </bean>
    <!-- DAO接口所在包名，Spring会自动查找其下的类 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.future.dao4ora.idao" />
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory4ora"></property>
    </bean>


	<!-- 配置Jdbc模板  -->
	<!--<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate"
		p:dataSource-ref="dataSource" />-->
		
	<!-- 配置事务管理器 -->
	<bean id="transactionManager4ora"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager"
		p:dataSource-ref="dataSource4ora" />
		
	<!-- 通过AOP配置提供事务增强，让service包下所有Bean的所有方法拥有事务 -->
	<aop:config proxy-target-class="true">
		<aop:pointcut id="serviceMethod4ora"
			expression=" execution(* com.future.dao4ora.service..*(..))
                or execution(* com.future.controller..*(..))" />
		<aop:advisor pointcut-ref="serviceMethod4ora" advice-ref="txAdvice4ora" />
	</aop:config>
	<tx:advice id="txAdvice4ora" transaction-manager="transactionManager4ora">
		<tx:attributes>
			<tx:method name="*" />

            <!--<tx:method name="insert*" rollback-for="Exception" />
            <tx:method name="save*" rollback-for="Exception" />
            <tx:method name="update*" rollback-for="Exception" />
            <tx:method name="delete*" rollback-for="Exception" />
            <tx:method name="*" read-only="true" rollback-for="Exception" />-->
        </tx:attributes>
	</tx:advice>

</beans>
